/**
 * The Collaboration module lets an extension send and receive ephemeral
 * broadcast messages between viewers running the same extension on the same
 * map, enabling multiplayer functionality for any use case.
 *
 * @module Collaboration
 */
export type { BroadcastEvent, BroadcastStateEvent, JsonValue } from "./types";

export type { CollaborationController } from "./controller";
